A CMSSideBar is a component to render the sidebar menu and its topics.

Instance Variables
	rootComponent:		<CMSRootElement>

rootComponent
	- the reference to the root component to have the possibility to change the main content.
